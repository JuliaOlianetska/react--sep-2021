export default function Post({post}) {
    return (
        <div>
            <ul>
                <li>{post.title}</li>
                body: <div>{post.body}</div>
            </ul>

        </div>
    );
}